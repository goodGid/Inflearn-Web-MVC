<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
  <display-name>Archetype Created Web Application</display-name>

  <!--
  기존에 Listener 말고
  Spring이 제공해주는 Listener를 사용하자
  = ContextLoaderListener

  아래 Listener 삭제
  <listener>
    <listener-class>be.goodgid.MyListener</listener-class>
  </listener>

  ContextLoaderListener가
  AnnotationConfigWebApplicationContext를 생성할 때
  AppConfig를 사용해서 만든다.

  context-param의 위치가
  filter보다 먼저 선언이 되어야한다.
  -->

  <context-param>
    <param-name>contextClass</param-name>
    <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
  </context-param>

  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>be.goodgid.AppConfig</param-value>
  </context-param>

  <filter>
    <filter-name>myFilter</filter-name>
    <filter-class>be.goodgid.MyFilter</filter-class>
  </filter>

  <filter-mapping>
    <filter-name>myFilter</filter-name>
    <servlet-name>hello</servlet-name>
  </filter-mapping>

  <listener>
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
  </listener>

  <!-- Add : Servlet Mapping -->
  <servlet>
    <servlet-name>hello</servlet-name>
    <servlet-class>be.goodgid.HelloServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>hello</servlet-name>
    <url-pattern>/hello</url-pattern>
  </servlet-mapping>

</web-app>
